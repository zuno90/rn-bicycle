import { Button } from "native-base"
import { ActivityIndicator } from "react-native"

const LoadingBtn: React.FC = () => {
  return <ActivityIndicator size="large" color="#966216" />
}

export default LoadingBtn
