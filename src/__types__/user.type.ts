export type TInputInformation = {
  name: string
  phoneNumber: string
  city: { label: string; value: number }
  district: { label: string; value: number }
  ward: { label: string; value: number }
  address: string
}
